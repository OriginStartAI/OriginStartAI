# First-Call Video Demo Script

This short demo script is for developers who want to test an OpenAI-compatible API endpoint before moving it into a production workflow.

## 30-second version

```text
Set your API key, base URL, and model.
Run one curl request.
Then copy the same pattern into Python or Node.js.
```

Example repo:

```text
https://github.com/OriginStartAI/openai-compatible-api-examples
```

Start link:

```text
https://originstartai.com?utm_source=video&utm_medium=demo&utm_campaign=video_demos&utm_content=github-demo-script
```

## 60-second version

1. Open the examples repo.
2. Copy `.env.example` to `.env`.
3. Set `ORIGINSTARTAI_API_KEY`, `ORIGINSTARTAI_BASE_URL`, and `ORIGINSTARTAI_MODEL`.
4. Run the curl first call.
5. Run the Python first call.
6. Run a JSON structured output example.
7. Move one real product prompt into the example.

New user offer: recharge `$5` and get `$5` extra API credits.
